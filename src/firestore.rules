rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla Piloto:
    // Permite todo (leer/escribir) SIEMPRE Y CUANDO el usuario esté autenticado.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}